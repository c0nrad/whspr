package main

const (
	// RemoteHost the remote host we are connecting to
	RemoteHost = "c0nrad.io"

	// RemotePort the remote port we are connecting to
	RemotePort = "1337"

	// Server run in server move
	Serve = false

	// KEY is a SUPER SECRET PRE-SHARED KEY. `echo "1337h4x" | md5`
	KEY = "3a989dba6fe6c87f"

	DisplayBanner = " whspr "

	TrustedCertPath = "./certs/trusted"
)
